#include <stdio.h>

/**
 * main - the entry point
 *
 * Description - the sum of a and b
 * @parameter - a and b
 * Return: Always 0 (success)
 */
int main(void)
{
	int a = 8, b = 7;
	int Sum = (a + b);

	printf("sum of a + b = %d\n", Sum);

	return (0);
}
